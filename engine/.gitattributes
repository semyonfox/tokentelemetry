# Generated by cmd/pricing-sync from models.dev and embedded into the binary.
# Committed deliberately (the tool makes no network calls at runtime), but it is
# machine-written data: collapse it in diffs and keep it out of language stats.
internal/pricing/data/pricing.json linguist-generated=true
